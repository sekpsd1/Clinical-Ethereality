# Project State

## Project

Clinical Ethereality

## Phase

Production Doctor smoke UAT passed; Full Doctor Flow UAT is paused until SlipOK credentials, privacy approval for `log=true`, and a separately approved Controlled Real-slip UAT are complete.

The project now contains a Next.js 15, React 19, TypeScript, and Tailwind CSS modular monolith with the reviewed Stitch screens, LINE LIFF/JWT authentication, Prisma/MySQL persistence, and role-protected customer, doctor, pharmacist, and admin workflows. The Doctor flow now includes assigned-patient detail access, full linked pre-consult assessments, persisted consultation payments, guarded appointment lifecycle transitions, audited structured prescriptions, in-app messages and notifications, and a Zoom Meeting SDK room foundation.

## Current Decisions

- Product category: clinical aesthetics commerce, consultation, pharmacy, and community web app
- Primary experience: authenticated platform for customers/patients, doctors, pharmacists, and admins
- Architecture pattern: modular monolith inside a single Next.js app
- Primary mutation pattern: Next.js Server Actions
- Server Action convention: new first-party mutations live in owning `features/*/actions.ts` modules, validate `FormData` with Zod before use, enforce session permissions before sensitive reads/writes, use Prisma transactions for multi-record changes, write audit logs for sensitive state transitions, and return shared typed form action state helpers from `lib/actions/server-actions.ts` for recoverable errors
- Route handlers reserved for LINE callback, webhooks, health checks, and integration endpoints
- Recommended frontend framework: Next.js 15 with App Router and React 19
- Recommended language: TypeScript
- Recommended database: MySQL
- Recommended ORM: Prisma
- Recommended styling: Tailwind CSS implementing the finalized Stitch design system
- Recommended backend approach: Next.js Server Actions
- Recommended authentication: LINE LIFF plus JWT
- User entry requirement: customers/patients must enter through the LINE Mini App/LINE LIFF experience; users without LINE must create or use a LINE account before accessing the app
- Primary account identity: LINE user identity is the required customer identity source; no standalone email/password or guest customer account path is planned for MVP, and email login is deferred until after the LINE Mini App MVP is complete
- Initial auth implementation: LINE LIFF ID token verification upserts a Prisma `User`, creates a Prisma `AuthSession`, and issues httpOnly JWT access and refresh cookies
- Session subject: JWT `sub` now uses the Prisma `User.id`; LINE identity remains stored on `User.lineUserId`
- Refresh session storage: refresh tokens are stored only as SHA-256 hashes on `AuthSession` records and are rotated on refresh
- Transparent protected-route refresh: middleware now attempts a valid refresh token before showing LINE Login when an access token is missing or expired, rotates the refresh token with a random JWT ID and database compare-and-swap, then returns with HTTP 307 to the exact original path and query so downstream layouts receive the new cookies. Middleware, the refresh API, and the LINE entry client use a short-lived httpOnly marker and bounded retry for concurrent CAS losers without sending replacement tokens to the losing request; if browser cookie synchronization still fails the flow stops with retryable HTTP 503/error state instead of LINE Login or a redirect loop. Only missing, expired, revoked, replayed, or identity-mismatched refresh sessions return 401 and re-enter LINE Login, while route-role mismatches go to the current database role's home. Verifier, configuration, network, database, and signing failures are not disguised as authentication failures. Access-token lifetime remains unchanged.
- Route protection: customer, admin, doctor, and pharmacist routes have middleware role boundaries. Cross-role access returns users to their own role home after any required refresh, while Admin retains the existing support access to Doctor and Pharmacist routes.
- Server-side role guard login returns: middleware forwards each protected request's exact local path and query to server rendering. If the server-side role guard must begin LINE Login, it uses that safe route-specific return path instead of a hard-coded Admin path; action and non-routed guard calls fall back to the appropriate role home.
- Local development access: `ENABLE_DEV_AUTH_BYPASS=true` enables `/api/auth/dev-session` and local customer/admin buttons on `/auth/line`; when seeded users are available, dev sessions use real Prisma user IDs so local write workflows can satisfy foreign-key constraints. This is disabled in production and does not replace LINE LIFF for MVP
- Local refresh E2E support: `/api/auth/dev-session` can persist an `AuthSession` only when explicitly requested in non-production mode and only when `DATABASE_URL` points to `localhost` or `127.0.0.1` with the approved `clinical_ethereality` or `clinical_ethereality_host_copy` database name. Remote databases and local tunnels to other database names are rejected.
- Local Plesk-copy Customer access: when the localhost database is a Plesk copy without `seed-line-customer`, the local-only Customer dev session now resolves the most recently active database customer instead of falling back to `dev:customer`. This fallback is restricted to `127.0.0.1`/`localhost` database URLs so it cannot select an arbitrary customer from a remote database.
- Local Plesk-copy staff access: the local-only Doctor and Pharmacist dev sessions can resolve the first active approved database-backed staff profile when seed IDs are absent, keeping foreign-key-backed QA usable without creating placeholder staff identities. Production remains unaffected.
- Admin foundation: `/admin` now has an admin-only shell and operational overview backed by Prisma counts for user approvals, consultations, payments, prescriptions, orders, inventory, and hidden community content when the database is available
- Admin user approvals: `/admin/users` now reads Prisma users with doctor/pharmacist profiles when a database is available, falls back to a DB-offline empty state, and includes admin-only Server Actions with inline success/error feedback for approving staff roles, suspending users, and changing an active account directly between customer and admin. Direct role changes cannot target the signed-in admin, cannot remove the last active admin, and are written to the audit log; doctor/pharmacist roles remain behind the license-review approval flow.
- Admin staff queue scaling: `/admin/users` now excludes ordinary customers, defaults to the pending-approval queue, separates pending/approved/inactive staff into status tabs, searches by name or LINE ID, reports database-wide status totals, and paginates filtered results at 20 records per page while retaining the existing review cards and actions.
- Staff invitation foundation: `/admin/users` now exposes invite links for doctor, pharmacist, and admin access requests; `/staff-invite/[role]` lets a signed-in LINE user submit a pending staff/admin access request without adding a new table, doctor and pharmacist requests require first and last name and save the combined name to the requesting account for admin review, and the existing admin approval queue remains the review surface. Doctor/pharmacist applicants stay active as customers while their staff profile is pending, see a Thai review confirmation after submission, and receive an in-app notification after approval.
- Admin payment review foundation: `/admin/payments` now reads Prisma payment records for both orders and consultations with the relevant customer/doctor context. Manual PromptPay review remains limited to order payments because consultation verification is provider-owned.
- Admin payment review clarity: `/admin/payments` now separates pending slip, pending review, verified, and rejected counts, uses readable Thai labels, and shows QR payload status, slip URL, review source/provider, provider result, transaction reference, verified amount, receiver name, reviewer, and reviewed timestamp when available
- Manual payment review service extraction: `features/payments/service.ts` now owns the sensitive manual payment review transition rules, payment/order updates, customer notification, inventory reservation consumption, and audit logging; a verified manual payment now remains `paid` until an Admin explicitly starts medicine preparation, matching provider-verified orders.
- Payment transaction-reference integrity: `Payment.normalizedTransactionReference` is an additive nullable unique field. Provider and Admin manual approvals both normalize references with NFKC, uppercase, and removal of whitespace/common separators before the final Serializable payment transition; the database unique index is the concurrency authority, so duplicate collisions fail closed before order, inventory, reward, notification, or audit side effects. Admin verification requires a reference, while pending/rejected and legacy records remain `NULL`; historic JSON evidence is deliberately not guessed or backfilled. Pre-deploy: take the normal database backup, confirm the migration is pending once, and do not populate the new field from `verificationPayload`; after deployment, verify the unique index exists and audit only the new normalized field for duplicate non-null values. Rollback: first return application code to a compatible version; remove the index/column only if the field is still entirely null, otherwise retain both to preserve verified/refunded integrity and audit history. Production migration/deployment remains pending.
- Manual Store refund MVP: Admin records a refund only after completing the external bank transfer. The additive Payment refund fields keep the incoming transaction reference unchanged and store a separate normalized unique outgoing reference, exact full amount, reason, actor, and timestamp. A Serializable transaction locks the Store order, allows only `paid`/`preparing` orders without shipped/delivered shipment history, uses Payment/Order CAS, cancels non-sent shipments, restores stock exactly once, creates an `adjust` compensating reward ledger even when the resulting balance is negative, and writes a customer notification plus bank-reference-minimized audit record. Re-running an already-complete refund is idempotent. Pre-deploy: take the normal database backup, confirm no duplicate non-null `normalizedRefundReference` values, apply the additive migration once, then verify the unique index and foreign key; do not backfill outgoing references from incoming evidence. Rollback: first use application code compatible with the additive columns; only remove the new index/foreign key/columns if all six refund fields are null, otherwise retain them to preserve completed refund evidence. Production deployment/migration remains pending.
- Admin order management foundation: `/admin/orders` now reads Prisma order, customer, item, linked doctor-issued prescription, payment, shipment, and fulfillment-audit records when a database is available, falls back to a DB-offline empty state, and includes admin-only Server Actions for moving orders through preparation, shipped, and delivered states. The screen shows prescribed medication details plus the Admin actor and timestamp for every fulfillment step.
- Customer order tracking foundation: `/store/orders` now reads the signed-in customer's Prisma orders, items, latest payment, and latest shipment records, falls back to a DB-offline empty state, and shows a compact mobile order timeline without changing the existing Stitch store screens
- Customer checkout foundation: `/store/checkout` now renders only the signed-in browser's real cookie-cart products, blocks unavailable/stale/out-of-stock/prescription-required carts and missing PromptPay configuration, re-reads price and stock on the server, reserves inventory with compare-and-swap updates, and creates Prisma order, order item, PromptPay payment, shipment placeholder, notification, and audit records before redirecting to `/store/orders`. A durable checkout request ID plus cart fingerprint prevents stale retries from clearing or reusing a different cart.
- Admin product catalog foundation: `/admin/products` now reads Prisma product and inventory context when a database is available, falls back to a DB-offline empty state, and includes admin-only Server Actions for creating and updating controlled product categories, short/full descriptions, usage, FDA number/status text, warnings, storage, fulfillment notes, prices, prescription/restriction requirements, and product status. Existing `description` remains the full description so current catalog records migrate without destructive renaming.
- Admin product image readiness: `/admin/products` now labels product images as already-uploaded image links, adds a local file-selection upload UX stub for product images, shows accepted local/storage URL examples, and warns testers not to place personal documents, slips, licenses, or other sensitive files in the product image field while real Cloudinary/S3 uploads remain owner-managed.
- Customer product browsing foundation: `/store` now reads active Prisma products with inventory context, preserves the reviewed Stitch marketplace layout, renders explicit empty/DB-unavailable states instead of saleable static fallback products, and links product cards into data-backed detail pages.
- Customer product detail foundation: `/store/[slug]` and `/store/paracetamol-500mg` now read active Prisma product and inventory context while preserving the reviewed Stitch detail layout. Missing/DB-unavailable products cannot be added to cart, out-of-stock products disable ordering, and the former sample Advice Log/product copy and dead share control are no longer presented as real data.
- Customer cart workflow foundation: `/store/cart` now provides a server-owned cookie cart with Prisma-backed product and inventory reads, quantity updates, clear-cart behavior, product-detail add-to-cart actions, and checkout submission using cart quantities before converting the cart into an auditable order/payment/shipment record. Stale cookie items are visible and block checkout until the cart is cleared.
- Customer order-from-prescription foundation: `/store/prescriptions/[prescriptionId]` now reads a customer-owned doctor-issued/order-ready prescription and shows only the product and quantity selected by the doctor from the clinic catalog; customers choose only a shipping address before creating the order and paying. The server derives every order line from the prescription's structured product IDs and quantities, never from customer-submitted product data. Prescription ordering requires a valid PromptPay payload before any order/reservation write, locks the owned prescription row to serialize duplicate submissions, reserves stock with compare-and-swap, and uses Serializable transactions.
- External prescription attachment metadata foundation: prescription-required product details now include a customer form with a local file-selection UX stub for owner-managed storage URLs, creating an order, payment, shipment placeholder, inventory reservation, reward/audit records, and a `FileAttachment` metadata row linked to the order without storing file bytes in the database; customer, admin, and pharmacist order queues show when an external prescription is attached
- Admin inventory management foundation: `/admin/inventory` now reads Prisma product and inventory records when a database is available, falls back to a DB-offline empty state, and includes admin-only Server Actions for updating stock quantity and low-stock thresholds
- Admin moderation foundation: `/admin/moderation` now reads hidden and archived article/comment records when a database is available, falls back to a DB-offline empty state, and includes admin-only Server Actions for restoring, hiding, or archiving community content
- Customer community article foundation: `/community/vitamin-c-tips` now reads a published Prisma article with visible comments and like counts, and includes customer-owned Server Actions for toggling article likes and creating visible comments
- Community post image upload: customer post creation/editing accepts one JPG, PNG, or WebP image up to 5 MB, compresses it in the browser, then revalidates, auto-orients, resizes to at most 1600px, strips metadata, and stores WebP output capped at 1.5 MB. File bytes remain in a private host directory configured by `COMMUNITY_UPLOAD_DIR`, metadata remains in `FileAttachment`, authenticated reads use `/api/community/images/[attachmentId]`, and replacement/removal remains owner-scoped and audited. No Prisma migration is required because the existing `Article.coverImageUrl` and `FileAttachment` fields are reused.
- Customer community empty-database continuity: when the fixed Stitch article is absent from a local Plesk copy, `/community/vitamin-c-tips` renders the reviewed Stitch fallback article with mutations disabled instead of returning 404. A present hidden or archived article still returns 404 so moderation remains effective.
- Customer content reporting foundation: `/community/vitamin-c-tips` includes customer report actions for articles and comments; reports are persisted without automatically hiding the reported content, active admins receive in-app notifications, and `/admin/moderation` remains the review surface.
- Admin notification foundation: `/admin/notifications` now reads recent Prisma notification records and active/pending recipients when a database is available, falls back to a DB-offline empty state, and includes admin-only Server Actions for sending in-app notifications
- Staff/admin copy cleanup: admin navigation, `/admin/notifications`, `/admin/moderation`, and `/admin/products` now use clearer Thai labels, empty states, action feedback, form copy, and status labels for the owner-facing operations surfaces.
- Admin customer oversight: `/admin/customers` now gives admins a Prisma-backed customer journey view covering assessment completion/expiry, recommended doctor, booked doctor, appointment status, and consultation-payment readiness. `/admin/customers/[customerId]` limits full health-assessment answers to the admin detail screen, includes appointment and prior-assessment history, and keeps pending staff applicants in the separate `/admin/users` workflow.
- Admin assessment reset: the customer detail screen lets admins make all currently active assessments for a customer expire so the next customer entry returns to the assessment flow. Existing assessment records and linked consultations remain intact, and the action writes an audit log instead of deleting health data.
- Test-flow reset safety: `scripts/reset-consult-flow-data.cjs` provides an operator-only, guarded reset for assessments, consultations/messages, slot locks, prescriptions, and related notifications. It requires an exact expected database/host and confirmation token, preserves audit history, users, products, inventory, and orders, and detaches prescription links from retained order items. The local `clinical_ethereality` database was backed up and reset on 2026-07-29; the Plesk database reset is still pending because production database access and a confirmed production backup are not available from this workstation.
- Assessment entry enforcement: direct customer access to `/consult` now checks for an active assessment and redirects to `/consult/assessment` when none exists, preventing the doctor list from bypassing the required assessment flow. Non-customer roles continue to their own role home.
- Customer notification foundation: `/notifications` now reads the signed-in user's Prisma in-app notifications, keeps the Stitch notification layout, maps notification types back to existing app destinations, and includes a customer-owned mark-read Server Action
- Customer reward foundation: `/profile/rewards` now reads Prisma reward ledgers and user balances, verified order payments and community comments award points through shared rules, and customers can spend 50 points for a wellness credit with a reward notification. Pending/rejected/provider-error payments do not award order points, and order reward creation is serialized per user.
- Doctor consultation, patient log, and prescription writing foundation: `/doctor/consultations`, `/doctor/patients`, and `/doctor/patients/[patientId]` read Prisma consultation, patient, assessment, message, payment, and prescription records scoped to the signed-in doctor profile. Patient-detail reads require an assigned consultation and write privacy audit metadata; list views mask LINE identity references.
- Pharmacist prescription queue foundation: `/pharmacist/prescriptions` remains a role-protected read-only reference for Prisma prescription, patient, doctor, consultation, and linked order-item records. Doctor-issued prescriptions are immediately order-ready and no pharmacist approval action is presented.
- Pharmacist medicine preparation foundation: `/pharmacist/orders` remains a role-protected read-only tracking view for paid, preparing, and shipped orders. Admin is the sole MVP operator for medicine preparation, packing, shipment, and delivery status mutations; the Pharmacist role and screens are retained without fulfillment permissions.
- Admin pharmacy fulfillment safety: Admin fulfillment transitions use compare-and-swap order updates, save the Admin actor on shipment records and audit logs, block shipment when a linked prescription is not order-ready, and non-destructively move linked doctor-issued prescriptions to `dispensed` when the order is shipped.
- Staff/admin queue copy polish: `/admin/inventory`, `/admin/payments`, `/admin/orders`, `/admin/schedules`, `/admin/notifications`, `/admin/compliance`, `/admin/products`, `/admin/users`, `/doctor/consultations`, `/doctor/patients`, `/pharmacist/prescriptions`, and `/pharmacist/orders` now reduce remaining English-facing operational copy while preserving existing role flows and Stitch-style surfaces.
- Reusable domain row extraction: shared `InfoTile` now replaces duplicated small data tiles across admin, doctor, and pharmacist operational screens while preserving the existing Stitch-style compact card treatment.
- Audit metadata foundation: Prisma now includes an `AuditLog` model and `/admin/audit` review screen; sensitive checkout, payment, fulfillment, prescription, moderation, inventory, product, reward, and staff-account actions write role-aware audit entries with entity metadata
- Admin schedule editor foundation: Prisma now includes `DoctorAvailability`, and `/admin/schedules` lets admins add, view, activate, or pause approved doctor availability slots with audit logging
- Admin schedule editing: each availability card now has a clear `แก้ไข` action that loads the existing doctor, weekday, time range, slot duration, notes, and active state into the same form for audited updates. The former icon-only power control now uses explicit `เปิดใช้งาน` / `ปิดใช้งาน` labels; pausing remains non-destructive and confirmed appointments are retained.
- Customer booking slot integration: `/consult/booking/somchai` now reads active `DoctorAvailability` slots for the selected approved active doctor and creates a pending-payment consultation from the selected slot before redirecting to consultation payment
- Plesk-copy customer booking compatibility: `/consult/booking/somchai` now selects the approved active doctor using the same database rule as the doctor list instead of requiring `seed-line-doctor-approved`. Customer doctor cards and booking profiles also fall back to the existing local doctor photo when host-only staff image bytes are unavailable, without changing the Stitch layout.
- Formal booking slot locking: consultation booking now creates a short-lived `ConsultationSlotLock` row with a unique `doctorId + scheduledAt` constraint before creating the pending-payment consultation, and the booking screen marks locked or already active consultation slots as unavailable so customers cannot select booked times
- Booking lock release: expired pending-payment slot holds are released by marking the consultation `cancelled`, disconnecting/deleting the slot lock, notifying the customer, and writing an audit log before booking/payment/appointment reads.
- Customer appointment detail foundation: `/consult/appointments/[consultationId]` now reads Prisma consultation data scoped to the signed-in customer, shows doctor, scheduled date/time, consultation status, fee, and routes the next CTA to consultation payment, waiting room, advice log, or rebooking based on status
- Consultation payment status foundation: `/consult/payment?consultation=...` now persists each consultation verification attempt in the shared `Payment` entity, retaining amount, QR/slip evidence, provider result, reviewed time, and verified/rejected state before moving verified consultations to `scheduled`.
- Doctor consultation lifecycle: `/doctor/consultations` enforces assigned-doctor/admin ownership and the guarded `scheduled -> live -> completed` transitions. Completion requires a clinical summary, and each transition creates patient notifications and audit records.
- Doctor live consultation: `/consult/live` reads only participant-owned persisted messages, removes demo records, polls for new in-app messages, and launches the authenticated Zoom client-view room when meeting metadata and Meeting SDK credentials are available.
- Zoom Production Pre-Credential Readiness: starting a consultation creates a waiting-room-protected meeting through Server-to-Server OAuth for the configured licensed `ZOOM_HOST_USER_ID`; only the assigned Doctor/Admin receives a role-1 30-minute Meeting SDK JWT plus a just-in-time ZAK, while the customer receives a role-0 JWT only for that customer's scheduled/live consultation. S2S/OAuth tokens are transient server-only, external join-URL fallback is disabled, signed webhook validation/replay idempotency never saves raw payloads, and `ZOOM_PRODUCTION_SETUP.md` records the owner-only configuration/UAT/rollback. Production still requires owner-managed Meeting SDK/S2S credentials and non-monetary end-to-end account validation.
- Doctor-issued prescription service extraction: `features/prescriptions/service.ts` now owns the sensitive prescription-writing rules for doctor ownership, consultation readiness, structured medication/dosage/quantity/instructions/warnings persistence, audit logging, and patient notification; customer and pharmacist views render the same medication structure.
- Doctor patient-log polish: `/doctor/patients` and `/doctor/patients/[patientId]` use Thai queue/history copy, full linked assessment answers, assigned-consultation history, privacy notices, and shortened patient references instead of raw LINE IDs.
- Doctor notifications: `/doctor/notifications` shows doctor-owned Prisma notifications with read/unread actions, and the doctor shell bell links to this data-backed screen.
- Customer prescription status foundation: `/consult/prescriptions` now reads Prisma prescriptions scoped to the signed-in customer, shows doctor-issued prescription readiness, clinic/order context, linked medicine order context, and next-step CTAs for advice log, prescription ordering, or order tracking
- Prescription-required purchase guard: prescription-required product details now route customers to order-ready prescription status instead of direct cart add, and normal checkout rejects prescription-required cart products unless they are ordered through the prescription-linked flow
- Latest client intake bundle: local owner-downloaded files received on 2026-06-08 outside the repo include one real doctor telemedicine profile/photo, home-test product catalog details and product images, clinic/facility license document image, payment/bank reference document, and corporate/certification images. Exact license, bank, tax, and personal document values must stay out of git and be configured or stored only through secure owner-managed channels.
- Clinic-as-pharmacy decision update: client confirms that a doctor-issued prescription can be used to place an order immediately without a pharmacist approval gate. Admin staff prepare medicine, pack, ship, and update fulfillment statuses. The Pharmacist role remains available but is not an MVP fulfillment mutator. In-app doctor-issued prescription ordering preserves prescription/order/payment/inventory/shipment/audit linkage; external prescription attachment metadata remains available for owner-managed storage URLs.
- Client-safe seed data update: the approved local doctor seed now uses the received doctor display profile, consultation fee, 15-minute online schedule, and official profile photo while keeping the exact physician license value redacted. The store seed now includes the received HPV/STIs home-test and self-swab catalog packages with product images, stock, low-stock threshold, VAT note, non-prescription flag, and FDA-pending descriptions; legacy demo products are archived on reseed.
- Local seed data: `prisma/seed.mjs` creates development admin, customer, pending/approved doctor and pharmacist, suspended customer, products, inventory, consultation, prescription, order, payment, shipment, moderation, notification, and reward records for testing admin queues
- Staff profile persistence: Prisma includes `Doctor` and `Pharmacist` profile models linked one-to-one with `User`, with license, status, approval, and basic workflow metadata
- Domain schema foundation: Prisma now includes consultation, prescription, product, inventory, order, order item, payment, shipment tracking, file attachment metadata, article, comment, like, notification, and reward point models with core status enums, ownership relations, and indexes
- Database migration foundation: Prisma initial migration `20260513120000_init` now captures the current MySQL schema for auth, staff profiles, consultations, prescriptions, commerce, inventory, payments, shipments, community, notifications, and reward records
- Initial role assignment: LINE LIFF sessions default to `customer`; doctor, pharmacist, and admin roles remain reserved for the future invitation/approval flow
- Permission enforcement direction: reusable server helpers in `lib/permissions/*` should be called from Server Actions and route handlers before sensitive reads or writes
- Integration route boundaries: `/api/auth/line/callback`, `/api/webhooks/payments`, and `/api/webhooks/zoom` handle external callbacks; Zoom webhook requests require a current signed HMAC and never persist the raw payload.
- Recommended video call integration: Zoom SDK
- Recommended payment approach: Thai QR plus automatic Slip Verification API using either SlipOK or EasySlip with API-key authentication
- Slip verification provider decision: SlipOK is the selected MVP provider; EasySlip remains a provider-neutral fallback and is not active. SlipOK Branch ID/API key, timeout, and endpoint override stay in server environment variables only and are never committed. The private-file adapter sends only multipart `files`, `amount`, and `log=true`; it never sends public/Drive URLs or `PAYMENT_UPLOAD_DIR`. SlipOK receiver names can be masked, so its Branch receiver validation (`log=true` / error `1014`) is authoritative instead of application-side exact name comparison. Production activation remains blocked on credentials, privacy approval because `log=true` sends and stores slip data with SlipOK, and a separately approved Controlled Real-slip UAT; OK BASIC has a hard limit of 100 slips/month and must upgrade to OK Start before exceeding it. See `SLIPOK_PRODUCTION_SETUP.md`.
- Slip verification API boundary: `/api/payments/verify-slip` accepts a payment ID plus QR payload, hosted legacy URL, or owned private attachment ID; it validates ownership/type/storage before reading private bytes, enforces customer ownership/admin payment-review permission, calls the configured provider client, updates payment/order status, and creates an in-app payment notification
- Provider slip verification service extraction: `features/payments/service.ts` now owns provider result mapping, guarded payment/order compare-and-swap transitions, one-successful-payment-per-order checks, verified/refunded transaction-reference reuse checks, verified inventory finalization, customer/reward notifications, hosted slip attachment metadata, duplicate attachment checks, and audit logging after SlipOK/EasySlip verification. Checkout metadata is preserved when verification results are merged; `/api/payments/verify-slip` enforces exactly one evidence source, returns persisted 30-second rejected-payment cooldowns with HTTP 429, leaves provider errors unpersisted, and stays focused on auth, validation, provider calls, and response shaping.
- EasySlip Production pre-credential readiness (Local only): the provider-neutral EasySlip adapter remains an inactive fallback, following documented v2 `POST /verify/bank` behavior with exact configured receiver matching. SlipOK is the active MVP decision; its credentials, privacy approval for `log=true`, and separately approved Controlled Real-slip UAT are the current Production gates.
- Customer slip upload UI: `/store/orders` and `/consult/payment` now expose the same customer-owned private upload panel for pending payments. It previews a selected local image and submits it directly to the server for private storage and manual review; it no longer asks the customer for a hosted URL or invokes provider verification from the upload UI.
- Private payment-slip Plesk MVP (Local only, 2026-08-09): Store and Consultation checkout upload JPG/PNG/WebP slips (maximum 5 MB) directly from the browser to the Node server. The server revalidates MIME type and file signature, writes bytes under absolute `PAYMENT_UPLOAD_DIR` outside the deployed application/document root, and records a private `FileAttachment` plus an authenticated `/api/payments/slips/[attachmentId]` route. There is no public URL: only the owning customer or an Admin with `payment:review` can read an attached file; cross-owner access returns non-enumerating `404`, and anonymous access returns `401`. Uploads move the payment to `pending_review`, retain audit/notification, stock-reservation, normalized transaction-reference, and retry boundaries. Store retains its existing Admin manual-review policy; Consultation remains provider-owned and cannot be manually approved or rejected by Admin. A Chromium Playwright UAT at `390×844` passed Store and Consultation UI upload, access boundaries, retry idempotency, stock preservation, Console/HTTP-500 checks, and cleanup against the isolated Local UAT MariaDB; only generated fixtures and simulated files were used. SlipOK private-file verification is now ready locally: server-side verification reads bytes from `storageKey` and sends multipart `files`, not a hosted URL or private path, while persisting only normalized result fields. This introduces no Prisma migration. Production remains blocked on owner configuration of the separate private directory with least-privilege Node access, credentials, privacy approval for `log=true`, and a separately approved Controlled Real-slip UAT because SlipOK has no sandbox/test fixture.
- Thai QR payment instruction foundation: `/store/checkout` now generates a dynamic PromptPay EMV QR payload for the order amount when `THAI_QR_PROMPTPAY_ID` is configured, stores the payload on the Prisma payment record, and `/store/orders` renders the saved QR/payment payload beside slip verification instructions
- Recommended deployment: Vercel app with managed MySQL
- Recommended storage: Cloudinary or S3-compatible object storage for attachments and images
- Recommended validation: Zod
- Recommended testing: Vitest and Playwright
- UI/UX source of truth: finalized Stitch design specification
- Frontend architecture priority: reusable mobile-first components for LINE LIFF
- State management direction: server-first with Server Components, Server Actions, React Hook Form for complex forms, URL state for filters, and minimal client context
- Final customer footer navigation: `Consult | Store | Community | Profile`
- Implementation priority: build the reviewed Stitch screens first, then add only supporting connector screens needed to complete the MVP flow. New pre-doctor assessment screen is now requested by the client and should wait for owner-provided Stitch HTML export before implementation.

## Architecture Summary

The app should be organized around domain modules rather than technical silos. Routes should stay thin, reusable UI should live in `components/*`, business logic should live in `features/*`, and cross-cutting services should live in `lib/*`.

Core layers:

- Presentation: mobile-first React routes and components
- Server actions: validated mutations and permission enforcement
- Domain services: role-aware business logic
- Data access: Prisma over MySQL
- Integrations: LINE LIFF, JWT, Zoom SDK, Thai QR, Slip Verification API, Cloudinary or S3, email, monitoring

Core route areas:

- Public products and articles
- Auth and LINE callback
- Authenticated customer app
- Doctor consultation workflow
- Pharmacist prescription and fulfillment workflow
- Admin operations
- Webhooks for payment and Zoom events

## Stitch Screen Inventory

Consult:

- Doctor list: implemented at `/consult`
- Pre-doctor assessment: implemented across `/consult/assessment`, `/consult/assessment/symptoms`, `/consult/assessment/duration`, and `/consult/assessment/complete`. Answers persist for 7 days, booking links the active assessment to the consultation, the queue shows a compact summary, and assigned-doctor patient detail shows the full stored answers.
- Doctor profile and booking: implemented at `/consult/booking/somchai`
- Consultation PromptPay checkout: implemented at `/consult/payment` with customer-scoped consultation reads, PromptPay QR generation, and provider-owned SlipOK MVP verification status handling; EasySlip remains inactive fallback
- Waiting room: implemented at `/consult/waiting-room`
- Live consultation: implemented at `/consult/live`, with persisted role-scoped messages and an embedded Zoom client-view route at `/consult/live/zoom`
- Advice log: implemented at `/consult/advice-log`
- Prescription verification status: implemented at `/consult/prescriptions`

Store:

- Health marketplace: implemented at `/store`
- Product detail: implemented at `/store/paracetamol-500mg`
- Store checkout: implemented at `/store/checkout`
- Payment success and tracking: implemented at `/store/payment-success`
- Customer order list and tracking: implemented at `/store/orders` as a supporting data-backed screen
- Order from prescription: implemented at `/store/prescriptions/[prescriptionId]`
- Customer checkout submission: implemented from `/store/checkout` into Prisma order/payment/shipment records

Community and Profile:

- User profile: implemented at `/profile`
- Reward points ledger: implemented at `/profile/rewards`
- Community hub: implemented at `/community`
- Create new post: implemented at `/community/create`
- Article/post detail and comments: implemented at `/community/vitamin-c-tips`
- Article likes and comments: implemented at `/community/vitamin-c-tips` through Prisma-backed Server Actions
- Notification center: implemented at `/notifications` and backed by customer-owned Prisma in-app notifications
- Community search results: implemented at `/community/search`

Admin:

- Admin dashboard shell: implemented at `/admin` as a role-protected static operational overview
- Admin user and role approvals: implemented at `/admin/users` as a role-protected Prisma-backed approval queue with DB-offline fallback, explicit customer/admin role editing for other active accounts, self-role protection, last-admin protection, and audit logging
- Admin payment review: implemented at `/admin/payments` as a role-protected Prisma-backed PromptPay review queue with manual verify/reject actions
- Admin order management: implemented at `/admin/orders` as a role-protected Prisma-backed fulfillment queue with manual preparation/shipped/delivered actions
- Customer order tracking: implemented at `/store/orders` as an authenticated Prisma-backed customer order list with payment and shipment timeline context
- Customer checkout foundation: implemented at `/store/checkout` with a guarded Server Action that creates pending payment-review order records
- Admin product catalog: implemented at `/admin/products` as a role-protected Prisma-backed catalog management screen with create/update actions
- Admin inventory management: implemented at `/admin/inventory` as a role-protected Prisma-backed stock queue with manual quantity and threshold updates
- Admin moderation: implemented at `/admin/moderation` as a role-protected Prisma-backed community content queue with restore/hide/archive actions
- Admin notifications: implemented at `/admin/notifications` as a role-protected Prisma-backed in-app notification sender and recent-notification review queue
- Doctor consultation queue and prescription writing: implemented at `/doctor/consultations` with persisted payment evidence, full assessment access, guarded lifecycle actions, room/chat access, and structured prescription submission.
- Doctor patient logs: implemented at `/doctor/patients` and `/doctor/patients/[patientId]` with assigned-consultation scoping and audited sensitive-record reads.
- Doctor notifications: implemented at `/doctor/notifications` as a role-protected Prisma-backed notification screen.
- Pharmacist prescription queue: implemented at `/pharmacist/prescriptions` as a role-protected Prisma-backed verification queue with manual verify/reject actions
- Pharmacist medicine preparation: implemented at `/pharmacist/orders` as a role-protected Prisma-backed fulfillment queue with manual preparation/shipped/delivered actions

## Decisions Still Needed

- Confirm exact MVP scope
- Choose managed MySQL host
- Choose Cloudinary or S3-compatible storage
- Confirm LINE LIFF channel setup
- Confirm JWT issuing, expiry, refresh, and revocation strategy
- Decide whether production will store protected health information
- Decide whether HIPAA-eligible vendors and BAAs are required from day one
- Decide whether customer product browsing and order tracking are public, authenticated, or hybrid
- Configure and validate production Zoom Meeting SDK and Server-to-Server OAuth credentials
- Configure selected SlipOK provider credentials/privacy approval and Controlled Real-slip UAT; retain EasySlip only as inactive fallback
- Decide whether Thai QR payments are manual-review MVP or fully automated after slip verification
- Community is included in MVP using the reviewed Stitch screens
- Decide whether delivery tracking is internal status only or carrier-integrated
- Define brand direction and visual identity
- Obtain or reference the finalized Stitch token/specification export before implementation
- Map Stitch colors, typography, spacing, radius, glass surfaces, shadows, and footer navigation into Tailwind
- Confirm whether any Stitch source files can be exported for exact tokens, images, and spacing values
- Collect client intake materials listed in `CLIENT_INTAKE.md`
- Confirm PDPA Privacy Policy, Terms of Service, health-data consent, teleconsultation consent, refund/cancellation policy, and community guidelines
- Confirm company legal name, tax ID, billing address, parcel sender address, and support contact
- Confirm doctor onboarding data, medical license, consultation fee, schedule, and official profile photo
- Confirm product catalog, FDA numbers, medical-standard product images, prices, stock, and prescription requirements
- Confirm pharmacy license data, pharmacist license data, and prescription verification SOP
- Confirm PromptPay account plus SlipOK Branch ID/API key through secure owner setup, privacy approval for `log=true`, and Controlled Real-slip UAT plan

## Proposed MVP

The first build should likely include:

- LINE LIFF login with JWT session handling
- Mobile-first authenticated app shell with persistent bottom navigation
- Stitch-based design system implementation
- Tailwind theme mapped from Stitch tokens
- Reusable UI primitives and domain components
- Final customer footer navigation: `Consult | Store | Community | Profile`
- Consult flow from doctor list through advice log
- Store flow from marketplace through payment success and tracking
- Community flow from hub through post creation, detail/comments, notifications, and search
- Profile hub with settings links
- Role-based access control and permission helpers
- Admin dashboard
- Customer/patient profiles
- Doctor users and profiles
- Pharmacist users and profiles
- Product catalog
- Inventory foundation
- Orders, order items, and order status tracking
- Thai QR payment instructions, slip upload, and payment review workflow
- SlipOK MVP verification API boundary with inactive EasySlip fallback
- Zoom consultation metadata and launch path
- Patient history captured through users and consultations
- Prescription creation
- Prescription verification
- Shipment tracking status updates
- Articles, comments, likes, reporting, and moderation foundation
- Notifications foundation
- Reward points foundation
- Audit metadata on sensitive entities

## Supporting Screens To Add Only As Needed

- Booking confirmation
- Payment pending
- Payment rejected
- Appointment detail
- Prescription verification status
- Order from prescription
- Order detail
- Saved articles
- Shipping addresses
- Settings

These screens should reuse Stitch-derived components and visual patterns from the 16 reviewed screens. Do not create unrelated new UI patterns.

## App Folder Structure

Use this structure when implementation starts:

- `app/`: route groups, layouts, pages, route handlers, and webhooks
- `components/`: reusable UI, layout, navigation, and domain presentation components
- `features/`: domain actions, queries, services, schemas, and feature-specific components
- `lib/`: auth, database, environment, permissions, validation helpers, design-system tokens, and integration clients
- `prisma/`: schema, migrations, and seed data
- `public/`: static assets and photography placeholders
- `tests/`: unit, integration, and Playwright tests

## API Plan

- Use Server Actions for first-party mutations.
- Use route handlers only for external callbacks and webhooks.
- Validate inputs with Zod at the server boundary.
- Enforce permissions in server code, not only in UI.
- Keep all sensitive prescription, payment, consultation, and order data behind authenticated routes.

## Frontend Architecture Decisions

- Follow the Stitch design system strictly.
- Do not redesign finalized UI.
- Use Stitch as the source of truth for Tailwind tokens and reusable components.
- Maintain one shared footer navigation implementation with `Consult | Store | Community | Profile`.
- Keep layouts mobile-first for LINE LIFF.
- Prefer server-first state management and avoid global client state unless justified.

## Current Implementation Handoff

Completed in the current frontend pass:

- Project scaffold: Next.js 15.5, React 19, TypeScript strict mode, Tailwind CSS, ESLint, Prisma placeholder schema, and `package-lock.json`.
- App shell: shared customer `AppShell`, global `TopAppBar`, and shared `FooterNav` for root customer areas.
- Design system foundation: Stitch-inspired tokens in `lib/design-system/tokens.ts`, variants helper, component contracts, and added shadows/backgrounds needed by the Consult screens.
- Consult doctor list: `/consult`, implemented from Figma with doctor cards, search bar, filter chips, local doctor images, and booking CTA.
- Doctor booking: `/consult/booking/somchai`, implemented from Figma with doctor bio card, calendar mock, time slots, and booking CTA.
- Consult payment checkout: `/consult/payment`, implemented from Figma with booking summary, PromptPay QR, slip upload mock, and payment submit CTA.
- Customer booking integration: `/consult/booking/somchai` keeps the reviewed booking layout while replacing static time buttons with active doctor availability slots from Prisma and a guarded Server Action that creates a pending-payment consultation.
- Customer appointment detail: `/consult/appointments/[consultationId]` adds the supporting booking confirmation/detail screen after a slot is booked, scoped to the signed-in customer and linked from consultation notifications.
- Customer consultation payment verification: `/consult/payment?consultation=...` verifies payment slips through the selected SlipOK MVP provider and moves only verified appointments into the waiting-room path; Consultation remains provider-owned.
- Customer prescription status: `/consult/prescriptions` adds the supporting status screen for doctor-issued prescription readiness and linked medicine order context, with notifications when doctors issue usable prescriptions.
- Customer order from prescription: `/store/prescriptions/[prescriptionId]` lets customers convert a doctor-issued/order-ready prescription into a linked medicine order without an extra document-review gate while preserving payment, shipment, reward, inventory reservation, and audit behavior.
- Waiting room: `/consult/waiting-room`, implemented from Figma with payment-confirmed status, countdown, doctor brief, preparation checklist, camera/mic test CTA, and join consultation CTA.
- Live consultation: `/consult/live`, implemented from Stitch zip reference with compact video shell, chat transcript, prescription attachment, composer, and end-call CTA.
- Advice log: `/consult/advice-log`, implemented from Stitch zip reference with doctor summary, medical note, prescription list, attachment, order medicine CTA, and PDF download CTA.
- Store health marketplace: `/store`, implemented from Stitch zip reference with custom marketplace header, search field, cart indicator, category cards, recommended product cards, and local product imagery.
- Store product detail: `/store/paracetamol-500mg`, implemented from Stitch zip reference with custom detail header, product hero, prescription-required warning, advice log selector, description card, and sticky buy CTA.
- Store checkout: `/store/checkout`, implemented from Stitch zip reference with order item cards, PromptPay QR payment summary, slip upload area, delivery address, and payment confirmation CTA.
- Store checkout submission: `/store/checkout` now uses a customer-scoped Server Action to create Prisma order, order item, payment, shipment, inventory reservation, and notification records, then redirects to `/store/orders`.
- Store payment success and tracking: `/store/payment-success`, implemented from Stitch zip reference with slip verification result, pharmacy preparation timeline, main-store return action, and pharmacy sync badge.
- Customer order tracking: `/store/orders` adds a supporting customer-owned Prisma-backed order list with payment state, shipment state, tracking number, and status timeline; `/store/payment-success` links into it.
- User profile: `/profile`, implemented from Stitch zip reference with custom profile header, teal verified member hero, settings menu rows, logout action, and app version note. The displayed name and avatar now come from the authenticated LINE-backed user, membership status comes from Prisma, advice count reflects completed consultations, and post count reflects published articles owned by that user.
- Reward points ledger: `/profile/rewards` establishes a customer-owned points balance and ledger view with earn/spend rules, a 50-point wellness credit redemption action, and DB-offline/empty states.
- Community hub: `/community`, implemented from Stitch zip reference with custom community header, search field, verified featured content card, category chips, and community feed cards.
- Create new post: `/community/create`, implemented from Stitch zip reference with custom compose header, topic/content inputs, image upload area, category selector chips, post CTA, and terms note.
- Article detail and comments: `/community/vitamin-c-tips`, implemented from Stitch zip reference with fixed detail header, hero article image, glass content card, interaction bar, comment list, and sticky comment composer.
- Article detail backend: `/community/vitamin-c-tips` now reads the published Prisma article seeded under `vitamin-c-tips`, renders visible comments and like counts, and supports customer like/comment Server Actions.
- Notification center: `/notifications`, implemented from Stitch zip reference with custom notification header, latest updates heading, unread notification glow, read cards, and Prisma-backed customer in-app notification records with mark-read actions.
- Community search results: `/community/search`, implemented from Stitch zip reference with search keyword, filter chips, result cards, author meta, likes, comments, and community footer context.
- Community data and ownership: the hub, URL-backed category filters/search, dynamic article detail, saved articles, likes, comments, and customer-owned create/edit flows now use authenticated Prisma data. Public customer authors use stable aliases, and only published articles plus visible comments are exposed.
- Community privacy and safety: post creation/editing requires explicit confirmation that no identifying personal or health information is included; customer reports are persisted without automatically hiding content, duplicate/self reports are blocked, and report outcomes are audited.
- Admin moderation connection: `/admin/moderation` reads pending Community reports with reason/context, supports keep/hide/archive outcomes, records reviewer metadata, and creates scoped notifications for reporters and affected owners without exposing LINE IDs.
- Community launch policy: `COMMUNITY_LAUNCH_POLICY.md` is the approved Thai-first source of truth for the initial post-moderation launch. It fixes the four existing categories, prohibits Before/After content, retains the five existing report reasons, sets a 24-business-hour privacy/danger triage target and two-business-day general-review target, routes appeals through ordinary support within seven days, and retains the 2,555-day audit target without introducing AI, pre-moderation, ticketing, or retention automation.
- Community local launch verification: a controlled 390×844 local-Docker UAT used only `[UAT]` text and a non-sensitive local image to verify authenticated post/comment/report flows, that reports do not automatically hide content, Admin hide/archive moderation, in-app notifications, and authenticated versus anonymous image reads. UAT content was archived afterward without deleting the retained report/audit history. Focused Community coverage plus the full unit suite, lint, typecheck, and production build passed. Production UAT was subsequently completed on 2026-08-09; see the completion record below.
- Community state handling: customer Community, saved articles, and Admin Moderation now include loading/error/empty handling; hidden or archived content has no public fallback, and `/profile/saved-articles` keeps the shared `FooterNav` Community tab active.
- Community Production UAT completion (2026-08-09): the `6a87222` Community launch-readiness release was deployed without a migration, rebuilt with `build:plesk-host`, and Node was restarted once. `COMMUNITY_UPLOAD_DIR` is an absolute private path outside the deployment/document-root, has least-privilege access and Node write access, and a generic `[UAT]` image upload/read succeeded. A fresh Plesk backup completed; the Google Drive backup/restore drill downloaded the Community-upload ZIP, matched its SHA-256 hash to the original, extracted it only into a temporary verification directory, and removed that temporary directory. This verifies the Google Drive procedure; Plesk restore-to-temp remains unavailable and was not represented as tested.
- Community Production operations and UAT completion (2026-08-09): Admin Media design owns moderation, checks the queue at 10:00 and 16:00 ICT on business days, and uses the approved 24-business-hour privacy/danger and two-business-day general-review targets. Appeals use the existing normal Support channel within seven days. Non-sensitive `[UAT]` content completed post, image, comment, cross-owner report, duplicate-report block, notification, audit, 390x844 mobile, Console/Network, Restore, Hide, and final Archive flows. Reports did not hide content automatically. An unauthenticated Incognito request received `401 Authentication required`; `/api/health` returned HTTP 200 with `status: ok`. The final UAT post is archived and is no longer available to Customers, while Audit and Report history remain. The no-content-change Keep outcome remains covered by focused Local tests; Restore, Hide, and Archive were exercised directly in Production. No AI, pre-moderation, ticket system, schema migration, or architecture change was added.
- Prisma auth models: `User` and `AuthSession` have been added with role/status enums, `lineUserId` uniqueness, session status, refresh token hash storage, and expiry indexes.
- Auth/session foundation: `/api/auth/line/session` verifies a LINE LIFF ID token through LINE, upserts the customer `User`, creates an `AuthSession`, and issues httpOnly JWT access/refresh cookies using the Prisma user ID.
- Session route handlers: `/api/auth/session`, `/api/auth/refresh`, and `/api/auth/logout` expose current-session, refresh, and logout boundaries without changing the existing Stitch frontend.
- LINE auth entry: `/auth/line` loads the LINE LIFF SDK, exchanges the ID token for an app session, attempts refresh first when possible, and redirects users back to their requested path. Normal app entry is role-aware through `/auth/role-home`: customers continue to the assessment flow, doctors open the consultation queue, pharmacists open prescription work, and admins open the operations dashboard; explicit safe internal destinations remain supported.
- Desktop LINE Login: `/api/auth/line/login` now starts the LINE OAuth authorization-code flow for regular browsers, protects the callback with a short-lived state cookie, exchanges the code only on the server, and reuses the existing Prisma/JWT session flow. Redirects now use the configured public `NEXT_PUBLIC_APP_URL` instead of a Passenger/internal request origin, preventing production callbacks from returning to localhost or `0.0.0.0`. LIFF app sessions remain the mobile-in-LINE entry path.
- Local dev bypass: `/api/auth/dev-session` can issue local-only customer/admin JWT cookies when `ENABLE_DEV_AUTH_BYPASS=true` and `NODE_ENV` is not production.
- Route protection: middleware protects `/consult`, `/store`, `/community`, `/notifications`, and `/profile`, with role boundaries prepared for future `/admin`, `/doctor`, and `/pharmacist` routes.
- Admin shell: `/admin` uses a dedicated responsive operational layout with persistent sidebar navigation and a logout action on desktop, the existing compact bottom navigation on mobile, and data-backed queue modules for role approvals, consultations, payments, prescriptions, orders, stock, moderation, and audit activity. Dashboard summary, risk, and queue cards link to their owning operational screens, while recent activity now reads the latest real `AuditLog` records instead of demo copy.
- Admin user approvals: `/admin/users` establishes the UI for role requests, current/requested role review, approve/suspend controls, approval safeguards, Prisma query structure, and Server Action boundaries.
- Admin payment review: `/admin/payments` establishes the first data-backed payment queue with order/customer context and manual verify/reject Server Actions.
- Admin order management: `/admin/orders` establishes the first data-backed fulfillment queue with order/customer/item/payment/shipment context and guarded status update Server Actions.
- Customer order tracking: `/store/orders` establishes the first customer-facing commerce read model with user-scoped order, item, payment, and shipment data plus DB-offline and empty states.
- Customer checkout foundation: `/store/checkout` establishes the first customer-facing commerce write path with permission enforcement, Prisma transaction boundaries, payment-review records, inventory reservation, and notification creation.
- Slip verification boundary: `/api/payments/verify-slip` establishes a provider-neutral route with SlipOK as MVP and inactive EasySlip fallback, using normalized verification results and service-owned guarded payment/order updates.
- Customer payment-slip upload UI: `/store/orders` and `/consult/payment` accept a local private slip image and submit it directly to the server. Hosted image URLs and client-side QR extraction are not part of this MVP upload path.
- Thai QR payment instructions: checkout and customer order tracking now use a generated PromptPay EMV payload and QR image for the exact order amount when `THAI_QR_PROMPTPAY_ID` is present; new orders remain pending payment until the customer submits a slip for verification.
- Admin product catalog: `/admin/products` establishes data-backed product catalog create/update actions for name, slug, description, image URL, price, prescription requirement, and status.
- Customer product browsing: `/store` now renders active Prisma products with inventory availability labels and provider-safe fallback content without redesigning the Stitch marketplace.
- Customer product details: `/store/[slug]` now renders active Prisma product detail data, with `/store/paracetamol-500mg` kept as a compatible existing route.
- Customer cart workflow: `/store/cart` now supports product-detail add-to-cart, cart quantity updates, clearing the cart, and checkout from cookie-cart contents into the existing Prisma order creation path.
- Admin inventory management: `/admin/inventory` establishes the first data-backed stock queue with product/inventory context and guarded stock update Server Actions.
- Admin moderation: `/admin/moderation` establishes the first data-backed community safety queue with article/comment context and guarded restore, hide, and archive Server Actions.
- Customer article interactions: `/community/vitamin-c-tips` establishes the first customer-facing community write path for article likes and comments with permission enforcement and revalidation.
- Customer content reporting: `/community/vitamin-c-tips` lets customers report articles and comments into the existing admin moderation queue without adding a separate reporting schema.
- Admin notifications: `/admin/notifications` establishes the first data-backed notification sender with active/pending recipient selection and recent notification review.
- Customer notifications: `/notifications` establishes the first customer data-backed notification center with user-scoped Prisma reads, DB-offline fallback, existing destination mapping, and a guarded mark-read Server Action.
- Customer rewards: `/profile/rewards` establishes the first reward-points read/write path with shared earning rules for orders and comments, spending rules for wellness credit redemption, balance updates, ledger entries, and notification creation.
- Doctor consultations: `/doctor/consultations` establishes the first doctor back-office queue with doctor-scoped consultation/patient/prescription context, DB-offline/profile-missing fallbacks, and guarded prescription note submission to pharmacist verification.
- Doctor patient logs: `/doctor/patients` establishes doctor-scoped patient history summaries from assigned consultations and prescriptions.
- Pharmacist prescriptions: `/pharmacist/prescriptions` establishes the first pharmacist back-office queue with prescription/patient/doctor/consultation context and guarded verify/reject Server Actions.
- Pharmacist medicine preparation: `/pharmacist/orders` establishes the first pharmacist fulfillment queue with paid/preparing/shipped orders and guarded preparation, shipment, and delivery status update Server Actions.
- Audit trail: `/admin/audit` lists sensitive action logs for admins with database-backed summary counts and 25-record pagination, backed by the Prisma `AuditLog` model and shared audit writer used by payment, order, prescription, moderation, inventory, product, reward, and user-management actions. Existing indexes cover actor, action, entity, and creation time; logs remain non-destructive until an approved retention/archive period is defined.
- Admin schedule editor: `/admin/schedules` provides an admin-only doctor availability editor for approved doctors, including weekday/time ranges, slot length, active/inactive status, and audit records for schedule changes.
- Permission foundation: reusable role and permission helpers live in `lib/permissions/*` for future Server Actions, route handlers, and domain services.
- Server Action convention helper: `lib/actions/server-actions.ts` now centralizes form action state, `FormData` conversion, and success/error helpers for new feature actions; the admin product catalog action has been migrated as the first reference implementation.
- Customer footer navigation consistency: customer app screens now use the shared `FooterNav` with final labels `Consult | Store | Community | Profile`; consult payment, waiting room, prescription status, and advice log keep `Consult` active through the shared footer, notification center remains a sub-screen without a root active tab, and live consultation hides the footer to reduce distraction.
- Assessment intro screen: `/consult/assessment` implements the first Stitch-exported assessment page as the app entry redirect from `/`, hides the customer footer for the focused intro canvas, and temporarily continues to the existing doctor list until remaining assessment pages arrive.
- Assessment symptom screen: `/consult/assessment/symptoms` implements the second Stitch-exported assessment page with progress, symptom option selection, focused bottom assessment controls, and customer footer hiding.
- Assessment duration screen: `/consult/assessment/duration` implements the third Stitch-exported assessment page with progress 2/3, duration option selection, focused bottom assessment controls, and customer footer hiding. The symptoms step now routes to this duration step after an option is selected.
- Assessment completion screen: `/consult/assessment/complete` implements the fourth Stitch-exported assessment page with the completion message, recommended-doctor preview cards, and CTA back to the consult doctor list. The duration step now routes to this completion step after an option is selected.
- Assessment data flow: `ConsultAssessment` now stores symptom and duration answers, recommendation topic/specialty/reason, completion time, and 7-day expiry. Active assessments are reused for app entry, attached to new consultation bookings, and surfaced in the doctor consultation queue before consult.
- Mobile LINE LIFF viewport verification: browser checks against `http://localhost:3001` covered the customer consult, consultation payment, waiting room, live consultation, advice log, prescription status, store, product detail, cart, checkout, order tracking, community, article detail, create post, search, notifications, profile, and rewards routes. The pass confirmed no visible app errors, shared footer availability on customer routes, fixed action controls clearing the footer on representative product/article screens, and live consultation remaining footer-free.
- Playwright smoke test foundation: `playwright.config.ts` and `tests/e2e/smoke.spec.ts` now run Pixel 5-sized smoke coverage for customer app routes, live consultation footer hiding, and admin/doctor/pharmacist role routes through the local dev auth bypass. `npm run test:e2e` passed with 21 smoke tests.
- Protected workflow integration test foundation: `tests/e2e/protected-workflows.spec.ts` now verifies unauthenticated customer and staff redirects preserve return paths, customers cannot enter admin/doctor/pharmacist workflows, doctor and pharmacist route boundaries stay separate, admins can support doctor/pharmacist queues, and sensitive slip verification rejects unauthenticated API calls. The targeted Playwright pass covered 6 protected workflow tests.
- Preview deployment configuration foundation: `vercel.json` now defines the Vercel Next.js build/install/output settings, Singapore region, and safe default auth/session environment values for hosted previews; `package.json` runs `prisma generate` after install so Prisma Client is available during Vercel builds; `DEPLOYMENT.md` documents preview, staging, production, health-check, and sensitive-environment requirements.
- Plesk deployment readiness foundation: `next.config.ts` now emits a standalone Next.js server, `npm run build:plesk` prepares and verifies `deploy/plesk`, and `PLESK_DEPLOYMENT.md` documents the owner-selected Plesk Node.js path with Node.js 20.20.2, `server.js` startup, `public` document root, environment variable checklist, standalone artifact copy, dry-run steps, hosted smoke checks, health checks, and rollback steps. The artifact now bundles Debian and RHEL/AlmaLinux Prisma query engines so a Windows build can safely make Prisma queries on the Plesk Linux host; LINE session failures log only safe step/error-code diagnostics server-side. Protected-route middleware uses the Next.js Node.js runtime so JWT verification reads the same Plesk runtime secrets as the authentication route handlers instead of creating a refresh/redirect loop. Vercel remains the preferred managed deployment path, while Plesk is acceptable only when persistent Node.js app support is available.
- Plesk remote QA guide: `PLESK_TEAM_TESTING_GUIDE.md` now gives off-site testers a hosted testing checklist for smoke checks, LINE/role access expectations, customer assessment/booking/payment, doctor assessment visibility/chat/prescriptions, store/prescription ordering, admin/pharmacist operations, sensitive-data rules, and bug reporting after the app is deployed to Plesk.
- Plesk migration handoff: `PLESK_MIGRATION_HANDOFF.md` now records the temporary cPanel proof-of-run result, the confirmed `/api/health` and LINE Login smoke checks, the CloudLinux/resource-limit caveats from cPanel, and the first-run checklist for moving the next hosted deployment to the client's new Plesk host without committing secrets.
- Plesk GitHub deployment: root `server.js` starts the standalone server generated by `npm run build:plesk-host`; the host build helper copies static and public assets into that output so Plesk Git deployment actions can run `npm ci` and the build without committing generated files. Initial Git deployment remains manual until the hosted smoke test passes.
- Staging environment configuration foundation: `STAGING.md` now defines the staging purpose, required isolated services, environment rules, database workflow, release checklist, and production promotion gates; `.env.staging.example` provides a non-secret staging variable checklist with dev auth disabled and dedicated staging placeholders.
- Production environment configuration foundation: `PRODUCTION.md` now defines launch gates, required production services, environment rules, deployment checks, data protection rules, rollback handling, and post-launch checks; `.env.production.example` provides a non-secret production variable checklist with dev auth disabled and production-only placeholders.
- Backup and restore readiness foundation: `BACKUPS.md` now defines environment-specific backup policy, database backup checklist, restore drill, object-storage backup checklist, incident restore procedure, recommended RPO/RTO targets, and production launch gates for backups before patient-like records are entered.
- Error monitoring foundation: Sentry Next.js monitoring is wired through `instrumentation.ts`, `instrumentation-client.ts`, server and edge Sentry config files, and `withSentryConfig` in `next.config.ts`; monitoring stays disabled until environment DSNs are configured, source-map uploads are gated by `SENTRY_AUTH_TOKEN`, and default PII capture remains disabled for privacy-sensitive workflows.
- Compliance review foundation: `/admin/compliance` now gives admins a production-readiness checklist covering PDPA/legal inputs, health-data consent, access controls, audit/backup/monitoring, clinical/pharmacy SOPs, payment credentials, and fulfillment requirements before real patient-like records are enabled.
- Consent acceptance foundation: `ConsentRecord` now stores versioned PDPA, Terms, health-data, teleconsultation, and prescription/pharmacy fulfillment acceptance records with timestamp, request metadata, and audit logging; `/profile/settings?section=privacy` exposes the current required consent set for authenticated customers.
- Pharmacy SOP draft: `PHARMACY_SOP_DRAFT.md` now captures the proposed prescription verification, medicine preparation, packing, shipment, exception, audit, and client-review flow for the clinic-as-pharmacy workflow without adding new implementation or changing architecture.
- Client SOP review message: `CLIENT_SOP_REVIEW_MESSAGE.md` now provides concise LINE and detailed email copy for the owner to send when requesting client review of the pharmacy/prescription fulfillment SOP.
- Intake templates: `DOCTOR_INTAKE_TEMPLATE.md`, `CONSULT_ASSESSMENT_INTAKE_TEMPLATE.md`, `PRODUCT_CATALOG_TEMPLATE.csv`, `CLIENT_PRODUCT_DATA_REQUEST.md`, and `PHARMACIST_INTAKE_TEMPLATE.md` now provide owner-sendable templates for collecting the remaining doctor, pre-doctor assessment, structured product catalog, and pharmacy/pharmacist data without committing sensitive exact values.
- Staff invite flow: `/staff-invite/doctor`, `/staff-invite/pharmacist`, and `/staff-invite/admin` create text-only pending review requests from authenticated LINE users, preserve admin approval as the final gate, and write audit metadata for invite requests. Staff review status is held on the doctor/pharmacist profile so the applicant's customer session remains usable until approval; successful submissions render a Thai pending-review state. Profile photos and license proofs are added by an admin during review. Public requests and admin staff approval use normal JSON/form API POSTs so Plesk ModSecurity does not require disabling the React Flight rule.
- Community card primitives: `ArticleCard` and `CommunityPostCard` now extract the existing Stitch community/search card treatments into reusable components without changing finalized layouts.
- Comment composer primitive: `CommentComposer` now extracts the existing Stitch article comment input treatment into a reusable form composer while preserving the article detail layout.
- Profile settings support: `/profile/settings` adds an authenticated customer settings support screen using the new `BottomSheet` primitive, linked from the existing profile gear control without changing finalized profile layout.
- Profile support screens: `/profile/saved-articles` and `/profile/shipping-addresses` add customer-facing support screens from the existing profile rows, keeping saved content and shipping placeholders behind authenticated customer routes until real client data is available.
- Customer profile/settings polish: `/profile`, `/profile/settings`, and `/profile/rewards` now use clearer Thai customer copy for membership, profile stats, consent status badges, reward balance, reward rules, and ledger expiry labels while preserving existing profile support-screen layouts. Profile account settings now show the authenticated user's stored name, member status, LINE connection, email, and phone instead of demo account values, and customers can update their own email and phone through a validated, permission-checked, audit-logged Server Action.
- Customer order detail support: `/store/orders/[orderId]` adds an authenticated customer order detail screen from the existing customer order tracking data and handles unknown IDs without exposing unrelated records.
- Permission unit test foundation: `vitest.config.ts` and `tests/unit/permissions.test.ts` now cover supported role detection, role-permission matrix expectations, admin permission coverage, assertion failures, own-record reads, and assigned-record reads. `npm run test:unit` passed with 27 tests.
- Stitch UI primitive test foundation: `tests/unit/ui-primitives.test.tsx` now covers shared glass surfaces, semantic status badge tone classes, final customer footer labels and active nested routes, live-consultation footer hiding, and placeholder composition with shared primitives. `vitest.config.ts` includes a narrow TSX transform for component imports while preserving the Next.js `jsx: preserve` compiler setting.
- Static assets copied into `public/images/doctors`, `public/images/profiles`, and `public/images/payments`.
- In-app consultation chat foundation is implemented separately from LINE chat: consultation messages are stored in Prisma/MySQL, sending is permission-checked against the consultation patient/doctor/admin scope, message creation writes audit logs and in-app notifications, `/consult/live` binds the existing Stitch-style chat UI to the latest consultation thread, and `/doctor/consultations` shows the latest chat message before consult.
- Admin compliance now includes an owner-managed integration readiness panel at `/admin/compliance` that checks PromptPay/payment webhook, SlipOK/EasySlip, file storage, LINE LIFF, and Zoom configuration presence without exposing secret values; the admin-facing readiness and launch-gate copy is now Thai-first.
- File storage foundation is implemented for owner-managed Cloudinary/S3 setup: storage readiness is centralized in `lib/storage`, hosted URLs are validated against the configured public/base URL when present, storage keys are extracted for metadata, and external prescription attachments store provider/configuration metadata. Payment slips use the completed private Plesk storage path instead of hosted URLs.
- Temporary private Plesk staff upload flow is implemented at both staff invitation and `/admin/users`: doctor/pharmacist applicants upload an official profile photo and license proof, admins can add or replace either file, approval is blocked until both are present, profile photos are delivered through a public application route for customer-facing doctor displays, license proofs require an authenticated admin session, and file metadata/audit history uses the existing `FileAttachment` model. Production requires `STAFF_UPLOAD_DIR` to point to a writable private directory outside the Git deployment root.
- Doctor access requests now use a controlled multi-select specialty list with an optional custom entry. Up to three normalized specialties are stored in the existing `Doctor.specialty` field, avoiding duplicate labels without a schema migration.
- Admin payment review clarity is implemented for `/admin/payments`, including readable Thai copy and evidence rows for QR payloads, slip URLs, provider/source/result, transaction reference, provider amount, receiver, and reviewer metadata.
- Local team testing guide: `TEAM_TESTING_GUIDE.md` is now readable Thai and covers localhost role login, customer assessment/booking/payment, doctor assessment visibility/chat/prescriptions, pharmacist fulfillment, admin operations, profile/community checks, bug severity, and report templates.
- Local database verification: local MySQL schema `clinical_ethereality` was pushed and seeded with `npm run db:push` and `npm run db:seed` using the project-owned Docker MySQL container `clinical-ethereality-db` on `127.0.0.1:3307`.
- Admin pharmacy fulfillment verification on 2026-07-31 passed lint, typecheck, 269 unit tests, the production build, and 3/3 focused Admin/Pharmacist Playwright cases against the local MySQL container. The broader parallel Playwright run passed 50/57; the remaining seven failures are existing Consult/logout/Doctor-notification expectation or timeout cases outside this pharmacy change.
- Local Plesk-copy Customer verification: localhost schema `clinical_ethereality_host_copy` was used through a process-level `DATABASE_URL` override without changing `.env.local`. Customer mobile E2E passed 32/32 serial cases with HTTP-success checks, Customer authorization integration passed 2/2 cases, focused Customer unit coverage passed 26/26 cases, and lint, typecheck, and production build passed on 2026-07-30.
- Doctor workflow verification on 2026-07-30 passed `npm run lint`, `npm run build`, `npm run typecheck`, `npx prisma validate`, 160 unit tests, 14 focused role/Doctor/Admin/Pharmacist E2E cases, and 4 final localhost smoke cases. The Plesk-copy database has no Prisma migration history, so only `20260730190000_complete_doctor_workflow` was applied directly to `clinical_ethereality_host_copy`; the production database was not changed.
- Customer Store safety verification on 2026-07-30 used only local MySQL at `127.0.0.1:3307`. A pre-sync dump was saved to `C:\tmp\clinical-ethereality-pre-store-safety-20260730.sql`, the local schema was synchronized to the existing Prisma schema, and no production database was connected or changed. Localhost checks confirmed active database products, non-saleable missing-product states, real cookie-cart behavior, checkout without sample QR/address/slip content, `/store/payment-success` redirecting to real order history, and the shared `FooterNav` keeping Store active across Store routes.
- Store payment verification now claims and preserves customer evidence before calling the provider, keeps provider outages in `pending_review` for existing Admin manual review, throttles retries, uses payment/order CAS, rejects duplicate provider transaction references, and fails closed unless the configured receiver matches exactly and the amount matches to the satang. Payment finalization also enforces the fixed Store reservation cutoff inside the Serializable transaction.
- Store stock reservations now expire after 30 minutes in `pending_payment` and 24 hours from order creation in `payment_review`; cleanup uses order/inventory CAS in Serializable transactions, preserves payment evidence, cancels pending shipment metadata, writes audit/notification records, and limits each customer to three active Store orders. Cleanup currently runs from Store customer actions and reads; a scheduled global worker is still required before production.
- Customer Store safety validation passed lint, typecheck, 247 unit tests, a 62-route production build, and all 8 Store/payment-focused Playwright cases. The broader Playwright run passed 54/55; its only failure is an out-of-scope Doctor notification test whose non-exact heading locator matches both `การแจ้งเตือน` and `ยังไม่มีการแจ้งเตือน`.
- Store shipping addresses are now customer-owned Prisma records managed at `/profile/shipping-addresses` through permission-checked Server Actions, Zod validation, Serializable transactions, user-row locking for a single default, and audit logs. Standard checkout plus internal/external prescription orders require an owned address and create an immutable `OrderShippingAddress` snapshot inside the existing order transaction. Customer order detail and Admin fulfillment read the snapshot, while legacy orders remain readable with an explicit missing-snapshot warning. The additive migration `20260731090000_add_shipping_addresses` leaves existing orders unchanged. Local verification on 2026-07-31 used only MySQL at `127.0.0.1:3307`, with a pre-sync backup at `C:\tmp\clinical-ethereality-pre-shipping-20260731.sql`; Prisma validate, lint, typecheck, 273 unit tests across 42 files, a 61-page production build, and authenticated localhost route smoke checks passed. Manual browser UAT passed 10/10, covering address creation/edit/default selection, ownership, checkout address enforcement, immutable snapshots, standard/internal/external-prescription orders, mobile Customer/Admin views, and legacy orders without snapshots. That local verification did not connect to or change Production.
- Production Shipping Address deployment, migration, and health check passed on 2026-07-31. A pre-migration database backup was downloaded as `bccgroup_app2026 (1).sql`; this backup must remain outside Git. The additive migration `20260731090000_add_shipping_addresses` was applied to the confirmed Production database and created `ShippingAddress`, `OrderShippingAddress`, and their foreign keys successfully. Production Shipping Address management UAT passed 7/7, covering first-address defaulting, second-address creation, editing, default switching, persistence after reload, the 390x844 mobile viewport without horizontal overflow, and no new Console/Server Error. Production Checkout and Order Snapshot UAT remains pending until the client provides product and stock data, so Store Production UAT is not complete. The Production database currently has no `_prisma_migrations` table; establish and approve a Prisma baseline before the next migration. Plesk dependency installation must use `npm ci --include=dev --no-audit --no-fund --legacy-peer-deps`.
- Controlled Production Store Checkout and Reservation Cleanup UAT passed on 2026-08-06 after a Pull/Deploy, `npm run build:plesk-host`, one Node App Restart, and an HTTP 200 `/api/health` check with `status: ok`. Customer `sekmon` completed the Product, Cart, Checkout, and Order flow using one controlled `[TEST] Gentle Cleanser 50 ml — ห้ามจำหน่าย` item at ฿199 with stock 20; the owned Shipping Address snapshot was correct and Order `CE-8JYOBW` was created without real payment or slip upload. Immediately after creation, stock was 20 / reserved 1 / available 19. After the pending-payment window elapsed, the owner-triggered cleanup ran successfully: the Order is `cancelled`, Payment `rejected`, Shipment `cancelled`, and stock returned to 20 / reserved 0 / available 20. Customer History still displays the cancelled order, and the expiration Notification plus Audit action `order.payment_reservation_expired` were present. Product, Cart, Checkout, Customer History, Notification, and the relevant Console/Network checks had no observed error, HTTP 403, or HTTP 500. This is a controlled test only: real client products/stock from Excel, EasySlip/payment-verification UAT, and a scheduled/global cleanup worker remain pending.
- Store global reservation cleanup worker is implemented as the server-only authenticated `POST /api/jobs/store-reservation-cleanup` route. It requires `STORE_RESERVATION_CLEANUP_SECRET` in the environment and a matching `x-clinical-job-secret` header, then delegates only to the existing 25-order batched `releaseExpiredStoreOrderReservations` service; that service retains the existing pending-payment/payment-review timeout rules, Serializable transaction, order-status CAS, reserved-stock release, payment/shipment transitions, audit log, and notification behavior. Focused and full unit coverage verifies expiry, payment-review expiry, rerun idempotency, concurrent CAS behavior, stock/payment/shipment/audit/notification effects, and unauthorized/unconfigured requests. Plesk Scheduled Task execution could not send the required HTTP request and remains disabled. `.github/workflows/store-reservation-cleanup.yml` is the replacement GitHub Actions scheduler: it runs every five minutes except minute zero, supports manual dispatch, has no checkout step, uses minimum permissions and non-cancelling concurrency, and sends the owner-managed GitHub Repository Secret `STORE_RESERVATION_CLEANUP_SECRET` only through the request header. GitHub Secret setup, workflow enablement, and Production UAT remain pending; no Production, Plesk, database, or order/stock change was made by this workflow definition.
- Transparent session-refresh validation on 2026-07-31 used only the project Docker MySQL database on `127.0.0.1:3307`. All 338 unit tests, 11/11 focused Playwright cases across customer/doctor/pharmacist/admin, lint, typecheck, and the 61-page production build passed. Coverage includes expired access with valid or invalid refresh, refresh replay and concurrent rotation, bounded middleware/API synchronization failure, LINE-only-on-401 policy, malformed JWT input, verifier/service failures, role mismatch, exact query preservation, role-safe destinations, redirect-loop prevention, and local-only dev-session enforcement. The Production deployment, environment, and database were not changed.
- Transparent Session Refresh was deployed to Production at `e6334d6` and the health check passed. Production Doctor UAT passed: after deleting only the access cookie, `/doctor/notifications?from=production-refresh-uat` retained its exact URL and query and did not enter LINE Login; `/doctor/notifications`, `/doctor/consultations`, and `/doctor/patients` all opened successfully. Observed Next.js RSC Fetch cancellations were `net::ERR_ABORTED` requests with a preceding HTTP 200 response and `canceled=true`, not failed destination requests. No Console or Server Error was observed. Full Doctor Flow UAT is paused until SlipOK credentials, privacy approval for `log=true`, and separately approved Controlled Real-slip UAT are complete.
- Latest Production Doctor UAT confirmed that Customer `sekmon` created a booking with Doctor Websthai successfully in `pending_payment`. The Doctor queue shows the booking and pre-consult assessment summary, while full patient detail, consultation room access, consultation-status transitions, and prescription writing are correctly blocked before payment confirmation. Production `/admin/compliance` reports PromptPay, the consultation verification provider, and payment webhook as missing; SlipOK/EasySlip setup remains incomplete and no Sandbox/Test mode is available. Do not continue Full Doctor Flow UAT using real payment until a safe Payment Integration/Sandbox path is provided.
- Doctor consultation queue cards now use the immutable nullable `Consultation.bookedDurationMinutes` snapshot as their primary source. New bookings copy the real `DoctorAvailability.slotMinutes` into this field inside the existing booking transaction, so duration remains available after expired/cancelled consultations release and delete their slot lock. Existing AuditLog behavior remains as a fallback only while the normalized field is `null`: the additive migration backfills the earliest valid explicit audit snapshot, then only recovers legacy `availabilityId` when `DoctorAvailability.updatedAt` is not newer than the booking audit; ambiguous records stay `ยังไม่ระบุ`. Local MariaDB 11.8.6 rehearsal used clones of the restored Production backup only: schema diff and Prisma status passed, existing row counts were retained, and synthetic fixtures proved safe legacy recovery while changed-availability legacy data remained null. Local verification passed 358 unit tests, a Pixel 5 Doctor queue no-overflow E2E test, lint, typecheck, and a 61-route production build. Production/Plesk and the Production database were not connected or changed.
- Doctor Queue Duration Production UAT passed after deploy commit `7ed3076`: Customer `sekmon`'s 3 August 2026 09:00 appointment displays `60 นาที`; the duration remains visible after expiry/cancellation and slot-lock deletion, and the mobile layout passed. Full Doctor Flow UAT remains paused before real payment because no Payment Sandbox is available.
- Prisma migration clean-replay verification on 2026-08-01 used only Docker MySQL at `127.0.0.1:3307` from the isolated `codex/prisma-migration-bom-fix` worktree. The initial migration contained a UTF-8 BOM that caused MySQL error 1064 on an empty database; only the leading `EF BB BF` bytes were removed, changing the migration checksum from `36d674221c4427d40ce0d4523cec7804a8c89d06d46bc089cbcec3e86ae71580` to `a8b12d8fb2f01bd39b386ef3c9d87cf390f416ce624569f566a7947d02f85867` without changing any SQL bytes. A read-only scan found the old checksum in the intentionally preserved failed Local database `clinical_ethereality_baseline_verify` with an unfinished migration record; no migration records were changed. The fixed repository chain replayed all 12 migrations into the new preserved Local database `clinical_ethereality_baseline_verify_repo_fix`; `prisma migrate status` reported up to date, schema diff reported no difference, and metadata inspection confirmed 29 tables including `_prisma_migrations`, 140 indexes, 47 foreign keys, and 23 enum columns. Metadata fingerprints matched the prior sanitized verification database. Production/Plesk was not connected or changed. The Production baseline remains pending; because Production currently has no `_prisma_migrations`, any later approved baseline must run from this fixed migration history, while databases already carrying the old checksum require a separate reviewed plan and must not have migration records edited ad hoc.
- Production Prisma baseline completed on 2026-08-01 against MariaDB `11.8.6`. The Restore-tested backup was `bccgroup_app2026_2026-08-01_15-09-40.sql.zip` (SHA-256 `42c3900d0b47eda3a754cb89cf9219ea7cac7652aed9c5eb5b8b51db7785cdfa`). Migration `20260727160000_add_structured_product_details` was applied successfully, adding Product fields `category`, `shortDescription`, `usageInstructions`, `fdaNumber`, `warnings`, `storageInstructions`, `controlledOrRestricted`, and `specialFulfillmentNotes`, plus `Product_category_idx`. The existing Production schema was then baselined with all 12 Prisma migrations: `_prisma_migrations` reports 12 finished and 0 rolled back; `prisma migrate status` is up to date; and the datasource/schema diff has no differences. Row counts across the original 28 application tables were preserved. Temporary `prisma/.env` and the temporary database user were removed after validation. The previously baseline-blocked Duration snapshot normalization work may now proceed, while Production Checkout UAT and Full Doctor Flow UAT remain pending.
- Production service recovery after the baseline passed: `run build:plesk-host` completed, then Node App Restart completed; `/api/health` returned HTTP 200 with `status: ok`. Unauthenticated checks for `/store`, `/consult`, and `/admin/products` redirected to LINE Login normally with no Server Error. This smoke test did not verify authenticated user flows. Operational note: restarting after the prior Node stop returned 404 twice; running `build:plesk-host` before Restart was required to restore service.
- Production booked-duration deployment completed at commit `21d7915` on 2026-08-02. The additive migration `20260801110000_add_consultation_booked_duration_snapshot` and deterministic safe backfill completed successfully; `_prisma_migrations` reports 13 finished and 0 rolled back. Prisma Client generation and the Production build passed. A transient HTTP 404 occurred after a Node App restart, but the runtime recovered and `/api/health` now returns HTTP 200 with `status: ok`.
- Authenticated Production Admin smoke testing with `Media design` passed for `/admin/users`, `/admin/products`, `/admin/inventory`, `/admin/orders`, and `/admin/compliance`. The pages read Production data without a Server Error; the Product form renders all fields added by Migration 9; and no HTTP 404/500 was found in the test-window request logs. Doctor `/doctor/consultations` remains accessible and `sekmon`'s booking shows the normalized 60-minute duration. Mobile viewport coverage and a direct browser Console stream have not yet been fully confirmed.
- Admin Schedule now has a Bulk Schedule Editor without changing the `DoctorAvailability` schema or existing edit/enable controls. An admin selects one approved doctor, multiple weekday chips, and multiple independently timed blocks, confirms the copy-to-selected-days preview, then creates every day/block pair in one serializable transaction. Server validation uses Zod and rejects invalid time order, non-divisible slot lengths, internal overlaps/duplicates, and overlaps/duplicates already saved for that doctor; every created availability record receives an audit entry. Local coverage includes batch expansion, multi-day/mixed-duration creation, permission enforcement, overlap/duplicate rejection, and transaction-failure rollback behavior. Typecheck, lint, and all 366 unit tests passed. The local `next build` compiled, type-checked, and generated all 61 pages, but its final standalone trace copy was blocked by Windows `EPERM` for the clean-worktree dependency junctions; this was a local test-environment limitation, not a source compile failure. Production/Plesk and Production DB were not touched.
- Manual Local UAT for Admin Schedule Date Overrides and Customer Booking passed on MariaDB 11.8.6 with the preserved UAT database. Websthai's recurring availability and the 8 August 2026 `17:00–18:00` special time produced eight distinct customer slots; the 9 August holiday produced no bookable slot. A single booking of the special `17:00` slot was created in `pending_payment`, then rendered as booked and disabled on a repeat visit. Admin attempts to deactivate or delete the special override after booking were blocked, preserving both the override and appointment. The calendar, special-time/holiday behavior, duplicate/overlap protection, mobile `390×844` layout, Console, and observed booking-page network responses passed. The Local dev server is stopped; MariaDB Local and all UAT records remain preserved; Production follow-up is recorded below.
- Production Schedule Date Override deployment and controlled UAT completed on 2026-08-08 at Production source commit `6359451`; Prisma migration `20260802160000_add_doctor_availability_date_overrides` completed as migration 14. Admin `Media design` verified the calendar, bulk editor, date-specific special time, and holiday workflow with Doctor `Websthai`. The controlled booking `cmsk69r0r000bgr2lsrmkls45` for the 17 August 2026 special slot expired/cancelled cleanly and released its slot; it is no longer active. The protected deactivate/delete checks passed while that booking existed. By explicit owner approval, the two UAT overrides remain in Production as example schedules: special availability `cmsk5qtn40001gr2lzs5fy6y8` on 17 August 2026 17:00–18:00 and holiday `cmsk5r4830005gr2l02fdd77s` on 18 August 2026. No payment, slip, or payment confirmation was used.
- Route-specific Role Guard Production deployment and UAT completed at source commit `72ae13b` on 2026-08-09. Plesk advanced from `226d9ef` through Pull/Deploy, `npm run build:plesk-host` passed, and the Node App restarted once; `/api/health` returned HTTP 200 with `status: ok`. No-cookie checks for `/admin/payments?status=pending&reviewer=me`, `/doctor/notifications?from=role-guard-uat`, and `/pharmacist/prescriptions?status=pending` each redirected to same-origin `/auth/line` with the exact encoded internal path/query, with no external redirect and no hard-coded `/admin` return. The existing authenticated Admin session opened `/admin/payments` and Admin support access to `/doctor/notifications` without Server Error, redirect loop, HTTP 403, HTTP 500, or loading failure. The only Console message was a browser-extension message-channel error, not an application error. Authenticated Doctor UAT then passed read-only with Doctor `websthai` in Chrome Incognito: `/doctor/consultations?from=authenticated-doctor-uat`, `/doctor/notifications`, and `/doctor/patients` stayed in the Doctor shell without LINE Login or Admin navigation; `/admin` redirected to `/doctor/consultations`; and the `390×844` viewport had no horizontal overflow, Server Error, redirect loop, HTTP 403/500, loading failure, or Console error. No consultation, prescription, message, or other business data was changed. Authenticated Pharmacist UAT remains deferred until an existing approved Pharmacist session is available; the Pharmacist role is read-only context, not an MVP fulfillment operator, and this deferral is not a Soft Launch blocker. No migration, Production database/data/role/account, secret, or Plesk configuration change was made.
- The localhost dev server was stopped after validation completed.

- Customer mobile UI polish pass 1 was completed in the isolated local worktree `codex/customer-ui-polish` from `origin/main`. At the 390x844 viewport, the first pass reduces oversized Store and Profile headers, card density, typography, and excess spacing; gives Consult booking/doctor cards and Store primary actions practical mobile sizing; tightens Cart and Checkout gutters; keeps existing safe-area padding and the shared FooterNav intact; and adds wrapping to the Doctor booking specialty row to avoid narrow-screen overflow. No Stitch layout, color/token, route, database, Prisma, payment, business logic, Production setting, or user-owned source change was modified.
- Customer mobile UI polish pass 1 was deployed to Production at commit `43debd4`. Read-only Production mobile UAT with Customer `sekmon` passed at `390×844` for Consult, Store, Cart, Checkout, and Profile: no horizontal overflow was observed; Store, Cart, Checkout, and Profile retained the shared FooterNav; and Consult correctly hid FooterNav behind the assessment gate. The UAT did not create or modify data.
- Customer cancellation for unpaid Store orders was completed in the isolated Local worktree based on `origin/main` commit `6db4902`, without Prisma schema, migration, architecture, Production, Plesk, or Production database changes. The customer-only Server Action validates the authenticated Customer session, permission, order ownership, latest `pending_payment` state, and absence of any verified/refunded payment. It reuses the existing Serializable transaction, order-status CAS, aggregated stock-release guard, pending-shipment cancellation, non-completed-payment rejection, AuditLog, and Customer Notification path from reservation cleanup; bounded retry handles Prisma write conflicts when customer cancellation races the Global Cleanup Worker, while reruns return an idempotent already-cancelled result without releasing stock or creating records twice. Cancelled orders remain in Customer History, and the Customer Order Detail shows the token-aligned `ยกเลิกคำสั่งซื้อ` action with native confirmation only while the latest read model is eligible. Local MariaDB integration coverage passed for ownership privacy, successful-payment blocking, exactly-once stock release, payment/shipment updates, audit/notification creation, rerun safety, and a real concurrent cleanup race. Mobile Local UAT at `390×844` passed with no horizontal overflow, a 44px full-width cancel action, working confirmation, and final Local database state `cancelled` / payment `rejected` / shipment `cancelled` / reserved stock returned / one audit / one notification for UAT order `cmskepbma0001un9shn5bnd90`; the cancelled UAT order remains in Local Customer History. Refund execution is intentionally not implemented and remains pending until Payment Integration/EasySlip is ready.
- Store Reservation Cleanup GitHub Actions automatic scheduled Run ID `31260602414` completed successfully; its Production cleanup endpoint response was successful. Customer Cancel Unpaid Store Order was deployed from commit `2d272d6`; Production `/api/health` returned HTTP 200 with `status: ok`. Controlled Production UAT with Customer `sekmon` created and cancelled test Order `CE-E3Y5GS` only: stock / reserved / available moved `20/0/20 → 20/1/19 → 20/0/20`; Order became `cancelled`, Payment `rejected`, Shipment `cancelled`; exactly one cancellation Audit Log and one Customer Notification were present; Customer History retained the order and the cancel action disappeared after reload. No real payment, slip, or refund was performed.
- Customer Cancel confirmation acceptance coverage is now protected by `tests/unit/customer-order-cancellation-confirmation.test.tsx`: the rendered form calls native `window.confirm`; a `false` result prevents the submit default and does not dispatch the Server Action, while a `true` result dispatches it exactly once. This is a focused Node component regression test; a browser-native-dialog UAT is optional and must explicitly inspect/dismiss or accept the JavaScript dialog if repeated. Real refund execution remains pending until Payment Integration/EasySlip is ready.
- Doctor-selected product ordering is implemented without a schema migration: the existing structured prescription items now persist the selected `productId` alongside medicine directions and quantity. The doctor queue lists only active prescription-required products with available stock, rechecks availability on the server before issuing the prescription, and records the canonical product name and integer quantity. Customer order creation accepts no product ID, derives and aggregates all mapped prescription items in a Serializable transaction, checks each current inventory row, and reserves each line with CAS. Legacy prescriptions without a product ID intentionally remain non-orderable and show a clinic-contact state; no name-based automatic matching is used. Focused unit coverage verifies that a tampered customer product field is ignored, the doctor-selected quantity is reserved, and the rendered customer screen has no product-selection field. No Production/Plesk/Production database access occurred.
- Local Browser + MariaDB UAT for doctor-selected prescription ordering ran at `390×844` on 2026-08-08 against only `127.0.0.1:3307/clinical_ethereality`. Controlled fixture key `doctor-rx-browser-1786205521976` used approved Local Doctor and Local Customer accounts, one active prescription product (`cmskkndjg0000unesm6br8mfu`, stock/reserved/available `10/0/10`), one archived product, one zero-stock product, a Local shipping address, and a legacy prescription without `productId` (`cmskknzam000bunr4fkuiifjy`). Doctor UI listed only the active in-stock product; issuing Prescription `cmskkrmny0001unkkl7d2iper` persisted the selected product ID, canonical product name, and quantity `2`. Customer UI displayed only that prescribed item and quantity, no product selector, and one shipping-address choice. A browser-injected `productId` for the archived product did not alter created Order `cmskkt98o0007unkkqwy577m7`; its OrderItem remained the prescribed active product at quantity `2`, moving inventory to `10/2/8`. Concurrent form submissions for Prescription `cmskkx9t30003unqolf45amw8` produced exactly one Order (`cmskkxevz000zunkklwbx8n0s`) at quantity `3`; the other request failed, with no duplicate reservation. All three Local UAT Orders were cancelled through the Customer UI, leaving orders `cancelled`, payments `rejected`, shipments `cancelled`, and inventory restored to `10/0/10`. Doctor and Customer mobile checks had no horizontal overflow, Customer `FooterNav` remained present, and observed Console/Network checks had no error or HTTP failure. No Production/Plesk/Production database access occurred.
- Local UAT retry blocker resolved: the customer prescription read model now ignores linked Orders in `cancelled` or `refunded` status and selects an active Order when one exists, while the existing Server Action, permission/ownership checks, and stock-reservation path remain unchanged. Regression coverage proves retry is available for cancelled-only/refunded-only history, remains hidden for `pending_payment`, `payment_review`, `paid`, `preparing`, `shipped`, and `delivered`, and prefers an active Order over an older cancelled one. Local Browser + MariaDB UAT created retry Order `cmsklib3k0001undkeylx42dn` from Prescription `cmskkrmny0001unkkl7d2iper`: inventory moved `10/0/10 → 10/2/8 → 10/0/10`; after Customer cancellation, Order was `cancelled`, Payment `rejected`, Shipment `cancelled`, and cancelled history remained visible. Customer mobile `390×844`, FooterNav, Console, and observed Network responses passed with no horizontal overflow, Console error, or HTTP failure. No Production/Plesk/Production database access occurred.

Known frontend caveats:

- Consult screens are still mostly static, except booking availability and appointment detail now read Prisma consultation data.
- Store marketplace and product detail now use Prisma-backed product data with non-saleable empty/error states; they no longer expose static fallback products as purchasable records.
- The local `clinical_ethereality` schema was synchronized and its active client catalog now renders on `/store`. The tested customer has no doctor-issued prescription or active prescription-required catalog product, so the prescription UI could only be verified through empty states plus focused action/unit coverage; no production data was used.
- Some deep-flow screens intentionally use custom headers based on Stitch references, while customer bottom navigation is centralized through the root `FooterNav` except for live consultation.
- `LiveConsultation` and `AdviceLog` were refined from Stitch `.zip` exports, not live Figma MCP, because the Starter plan MCP read quota was exhausted.
- Running `next build` while `next dev` is active can corrupt `.next` dev chunks on this Windows setup. After builds, stop dev server, clear `.next`, and restart `npm run dev -- -p 3001`.

Not implemented yet:

- Authenticated Pharmacist UAT of route-specific server-side role guard LINE Login return paths remains deferred until an existing approved session is available. The implementation was merged into `main` at `ba50d6f` and deployed at `72ae13b`; Doctor UAT passed, middleware forwards the protected route path/query, and guards retain role-specific fallbacks without changing transparent session refresh behavior. The read-only Pharmacist deferral is not a Soft Launch blocker.
- Business-domain queries beyond the current admin, doctor, pharmacist, customer notification, customer consent, customer order tracking, and customer article interaction foundations.
- Domain service coverage has started with shared order fulfillment transitions for admin/pharmacist workflows, doctor-issued prescription rules, consultation payment verification transitions, manual payment review rules, and provider slip verification persistence for payment/order updates, customer notification, hosted slip attachment metadata, and audit logging; additional services should be added as more sensitive workflows are implemented.
- Reusable mobile layout primitives now include `Screen`, `SafeArea`, `Button`, `IconButton`, `TextField`, `SearchField`, `EmptyState`, `InfoTile`, `PaymentStatusBadge`, `OrderTrackingTimeline`, `NotificationItem`, `ProfileSettingsItem`, `ArticleCard`, `CommunityPostCard`, `CommentComposer`, and `BottomSheet`; continue extracting unchecked Stitch primitives without redesigning finalized screens.
- Broader data-backed management screens, doctor screens, and pharmacist screens still need implementation behind the prepared role boundaries.
- Final pre-doctor consult assessment recommendation mapping now uses the received real doctor profile/specialty, sends completion users to `/consult?recommended=assessment`, and highlights the approved doctor from the active assessment while still allowing manual doctor choice.
- Broader booking integration now has database-backed slot locking for customer booking with automatic release of expired pending-payment holds; future refinements can add explicit customer cancellation and richer admin schedule management.
- Configure the private payment-slip directory on Plesk with least-privilege Node access before Production configuration; provider credentials and non-monetary provider UAT remain separate pending work.
- Explicit prescription-item-to-product mapping and a database uniqueness constraint for one order per doctor-issued prescription; current no-schema safeguards serialize the owned prescription row and prevent the observed duplicate path but cannot infer a medication/product match safely.
- Refund stock release and real refund execution remain pending until Payment Integration/EasySlip is ready; unpaid customer cancellation now has separate exactly-once release rules and must not be treated as a refund.
- A normalized bank transaction-reference field and database uniqueness constraint for Admin manual payment verification; provider-verified references are guarded now, but manual approval cannot safely guarantee cross-order slip uniqueness without this persisted identifier.
- Actual upload plus review gating for external prescriptions. The current external-prescription form still accepts an owner-managed hosted URL and creates order/payment/shipment metadata immediately without a pharmacist verification gate, per the previously recorded clinic workflow decision.
- Data-backed Store category filtering/search and carrier tracking integration; the current category/search controls remain presentation-only and carrier events remain internal shipment records.
- Production PromptPay/bank reference data has been received and must be configured securely through environment secrets such as `THAI_QR_PROMPTPAY_ID`; exact values must not be committed. Dynamic Thai QR payload generation is implemented for store checkout orders.
- Production consultation payment verification uses the selected SlipOK MVP: configure `SLIP_VERIFICATION_PROVIDER=slipok`, `SLIP_VERIFICATION_API_KEY`, `SLIPOK_BRANCH_ID`, and `SLIPOK_REQUEST_TIMEOUT_MS` securely. `log=true` sends and stores slip data with SlipOK, so privacy approval is required. EasySlip remains inactive fallback. Production is blocked until credentials, privacy approval, and a separately approved Controlled Real-slip UAT are complete; see `SLIPOK_PRODUCTION_SETUP.md`.
- Production Zoom Meeting SDK and Server-to-Server OAuth credentials are not configured yet, so real meeting creation/camera/microphone UAT remains pending. The local pre-credential integration audit is complete; before Production UAT the owner must provision the two apps, scopes/events/HTTPS webhook, a licensed host user (and enough host capacity for any overlapping consultations), then follow `ZOOM_PRODUCTION_SETUP.md` without using a monetary payment flow.
- Realtime chat transport/provider such as Firebase, Pusher, Ably, or WebSocket/SSE has not been selected or integrated yet; current MVP foundation persists messages through the existing Next.js/Prisma/MySQL architecture.
- Actual Cloudinary/S3 upload provider integration remains pending only for external prescription attachments and broader attachments. Payment slips use completed private Plesk storage; payment provider activation remains blocked on SlipOK credentials, privacy approval for `log=true`, and separately approved Controlled Real-slip UAT.
- First hosted Plesk smoke deployment is in progress on `https://app.bccgroup-thailand.com`: HTTPS, `/api/health`, Git/Node 24 deployment, and mobile LINE LIFF login are passing. Deploy and verify the desktop LINE Login OAuth callback, approve dedicated staff roles, and complete the end-to-end UAT flow before launch approval. The temporary cPanel host proved the standalone app and LINE Login build path but is not the preferred ongoing deployment target because of shared-host resource limits.
- Community images can temporarily use private Plesk host storage through `COMMUNITY_UPLOAD_DIR`; migrate file bytes to Cloudinary/S3-compatible object storage before host capacity or multi-instance deployment makes local disk unsuitable. The Community rules/moderation policy is approved and documented in `COMMUNITY_LAUNCH_POLICY.md`. Doctor production work now mainly remains Zoom credential/UAT validation and deciding whether polling should be replaced by a dedicated realtime transport.
- Community Soft Launch operational gates are now complete with the verified Google Drive backup/restore procedure and controlled Production UAT. A Plesk-native restore-to-temp capability remains a future hardening item, not a completed drill.
- Automated tests beyond the current permission unit coverage, feature validation schema coverage, Stitch UI primitive coverage, protected workflow integration coverage, Playwright smoke coverage, lint, build, and typecheck.

## Risk Notes

- Compliance requirements affect vendor selection and architecture
- Consultations, prescriptions, payment records, order records, shipment tracking, reward point adjustments, and images should be treated as sensitive records
- Ownership and role scope should be represented on the main entities without expanding the database beyond the agreed entity list unless needed
- AI features should wait until permissions, audit logging, and data boundaries are established
- Payments, prescriptions, and pharmacy fulfillment introduce additional regulatory and operational complexity
- `npm audit --omit=dev` reports 11 runtime advisories (0 critical, 4 high, 6 moderate, 1 low) in existing Next.js/Sentry/transitive packages; no Zoom package advisory was reported. Dependency upgrades should be handled as a separate tested maintenance task.

## Workspace Notes

- The workspace contains planning documentation and is initialized as a git repository
- Latest pushed planning commit: `18b9ab2 Document Stitch navigation plan`
- Application framework scaffold has started with Next.js 15, React 19, TypeScript, and Tailwind CSS
- Dependencies have been added through npm with `package-lock.json`
- Prisma has a placeholder MySQL schema only; domain models and migrations are deferred until the MVP entity boundary is confirmed
- Customer app routes currently redirect from `/` into the Stitch navigation shell at `/consult`
- Shared customer footer navigation is implemented with the final tabs: `Consult | Store | Community | Profile`
- Latest implemented frontend area: customer Consult flow from doctor list through advice log

## Next Recommended Step

Phase 12 quality work is complete, with hosted Plesk deployment now in progress at `https://app.bccgroup-thailand.com`. HTTPS, `/api/health`, Git deployment, Node 24 runtime, MySQL schema, and mobile LINE LIFF login are working. Next recommended step: deploy and verify desktop LINE Login OAuth, approve and test dedicated staff accounts, then run the documented customer-to-doctor-to-admin UAT flow. Before launch, still configure SlipOK credentials and privacy approval/Controlled Real-slip UAT, external prescription attachment storage, Zoom, final FDA numbers, and decide whether realtime chat is required. Use `PLESK_TEAM_TESTING_GUIDE.md` for the full internal checklist and `CLIENT_UAT_GUIDE.md` for the client-facing acceptance review.
